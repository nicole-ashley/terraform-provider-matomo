---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "matomo_tagmanager_tag_customhtml Resource - terraform-provider-matomo"
subcategory: ""
description: |-
  Allows you to embed any custom HTML, for example JavaScript or CSS Styles.
---

# matomo_tagmanager_tag_customhtml (Resource)

Allows you to embed any custom HTML, for example JavaScript or CSS Styles.

## Example Usage

```terraform
provider "matomo" {}

resource "matomo_site" "example" {
  name = "Example CustomHtml Site"
  urls = ["https://example-customhtml.example.com"]
}

resource "matomo_tagmanager_container" "example" {
  site_id = matomo_site.example.id
  context = "web"
  name    = "Example CustomHtml Container"
}

resource "matomo_tagmanager_trigger" "example" {
  container_id = matomo_tagmanager_container.example.id
  type         = "PageView"
  name         = "Example CustomHtml Trigger"
}

resource "matomo_tagmanager_tag_customhtml" "example" {
  container_id = matomo_tagmanager_container.example.id
  name         = "example-customhtml"
  fire_trigger_ids = [matomo_tagmanager_trigger.example.id]
  custom_html = "example-value"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `container_id` (String)
- `custom_html` (String) This tag is ideal when you need to add for example custom styles or custom JavaScript or when you are looking for a specific tag which is not yet supported. With this tag you can append any HTML to the bottom of your page, add styles, or execute JavaScript. Note: You can replace content within the HTML with variables by putting a variable name in curly brackets like this {{PageUrl}}.
- `fire_trigger_ids` (List of String)
- `name` (String)

### Optional

- `block_trigger_ids` (List of String)
- `html_position` (String) Define the position of where the HTML should be inserted into your website.
- `status` (String)

### Read-Only

- `id` (String) The ID of this resource.
