---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "matomo_tagmanager_tag_honeybadger Resource - terraform-provider-matomo"
subcategory: ""
description: |-
  Exception and Uptime Monitoring for Application Developers
---

# matomo_tagmanager_tag_honeybadger (Resource)

Exception and Uptime Monitoring for Application Developers

## Example Usage

```terraform
provider "matomo" {}

resource "matomo_site" "example" {
  name = "Example Honeybadger Site"
  urls = ["https://example-honeybadger.example.com"]
}

resource "matomo_tagmanager_container" "example" {
  site_id = matomo_site.example.id
  context = "web"
  name    = "Example Honeybadger Container"
}

resource "matomo_tagmanager_trigger" "example" {
  container_id = matomo_tagmanager_container.example.id
  type         = "PageView"
  name         = "Example Honeybadger Trigger"
}

resource "matomo_tagmanager_tag_honeybadger" "example" {
  container_id = matomo_tagmanager_container.example.id
  name         = "example-honeybadger"
  fire_trigger_ids = [matomo_tagmanager_trigger.example.id]
  honeybadger_api_key = "example-value"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `container_id` (String)
- `fire_trigger_ids` (List of String)
- `honeybadger_api_key` (String) The apiKey of your honeybadger project
- `name` (String)

### Optional

- `block_trigger_ids` (List of String)
- `description` (String) Optional free-text description, shown in Matomo's Tag Manager UI.
- `honeybadger_environment` (String) The environment where the container is included (production, dev, etc.). Leave empty to default to the environment of this container.
- `honeybadger_revision` (String) (optional) the version of your project
- `priority` (Number) Execution priority - lower values fire earlier when multiple tags fire on the same trigger. Matomo defaults to 999 when unset.
- `status` (String)

### Read-Only

- `id` (String) The ID of this resource.
